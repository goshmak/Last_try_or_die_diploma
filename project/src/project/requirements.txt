# requirements.txt
# Notification Module -- ANO VO "Humanitarian University"
#
# Install with:
#   pip install -r requirements.txt
#
# All packages are cross-platform (Linux, macOS, Windows).

# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Settings / validation ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Database (async) ---
sqlalchemy==2.0.36
aiosqlite==0.20.0          # SQLite async driver (prototype)
asyncpg==0.30.0            # PostgreSQL async driver (production)

# --- Redis queue ---
redis==5.2.1               # redis-py with asyncio support (redis.asyncio)

# --- HTTP client (VK API, API Gateway) ---
httpx==0.27.2

# --- Email (async SMTP) ---
aiosmtplib==3.0.1

# --- Templating ---
jinja2==3.1.4

# --- Utilities ---
python-dotenv==1.0.1       # .env file loading (used by pydantic-settings)
